import React from 'react';

type Props = {
    handleSubmit: () => void;
};

// (event: React.FormEvent<HTMLFormElement>) => void React form onSubmit.

const HeroForm: React.FC<Props> = () => {
    return <></>;
};

export { HeroForm };
